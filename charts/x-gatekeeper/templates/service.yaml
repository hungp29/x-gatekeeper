apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-gatekeeper.fullname" . }}
  labels:
    {{- include "x-gatekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "x-gatekeeper.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
